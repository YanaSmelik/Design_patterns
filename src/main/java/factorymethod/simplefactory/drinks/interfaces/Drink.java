package factorymethod.simplefactory.drinks.interfaces;

//The Drink declares the interface, which is common to all objects
// that can be produced by the creator and its subclasses.
public interface Drink {
}
